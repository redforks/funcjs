/**
 * Return true if value is truthy.
 *
 * @param {any} v
 * @returns
 */
export function truthy(v) {
  return !!v;
}

/**
 * Return true if value is falsy.
 *
 * @param {any} v
 * @returns
 */
export function falsy(v) {
  return !v;
}
